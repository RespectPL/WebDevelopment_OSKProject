<div class="container-fluid">
    <div class="row my-4 mx-auto text-center">
        <p align="right">Jesteś zalogowany jako <b>{{ login }} <a href="/users/logout" class="btn btn-dark">Wyloguj się</a></b></p>
        <p align="left"><a href="/participants" class="btn btn-info">Powrót do głównego panelu kursanta</a></p>
    </div>
    <div class="row my-4 mx-auto text-center">
        <h1><b>PANEL KURSANTA</b></h1>
        <h5><b>Ustalone egzaminy wewnętrzne</b></h5>
    </div>
    <div class="row my-4 mx-auto text-center col-8">
        {{#if cdp}}<div class="alert alert-danger" role="alert">Masz niezaliczone egzaminy wewnętrzne!<br />Powodem mogła być Twoja nieobecność
            lub Twój instruktor nie zweryfikował jeszcze wyników egzaminu. Skontaktuj się z nim!</div>{{/if}}

        {{#if cs}}<div class="alert alert-success" role="alert">Masz ustalone egzaminy, których termin się zbliża! Sprawdź dane i terminy!</div>{{/if}}
    </div>
    <div class="row my-4 mx-auto text-center">
        <table class="table">
            <thead class="table-primary">
                <tr>
                    <th scope="col">Instruktor</th>
                    <th scope="col">Kurs</th>
                    <th scope="col">Termin</th>
                    <th scope="col">Stan</th>
                </tr>
            </thead>
            <tbody>
                {{#each items}}
                <tr>
                    <td>{{this.instruktor_imie}} {{this.instruktor_nazwisko}}</td>
                    <td>{{this.kurs_info}}</td>
                    <td>{{this.data}}</td>
                    <td>{{#if this.completed}}
                            {{#if pass}}<font color="green">Zaliczony</font>
                            {{else}}<font color="red">Niezaliczony</font>
                            {{/if}}
                        {{else}}
                            <font color="navy">Nadchodzący</font>
                        {{/if}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>